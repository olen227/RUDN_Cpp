# JSON Parser - Курсовая работа
## C++17 JSON Parser с std::variant

---

## 🚀 Быстрый старт

### 1. Сборка проекта

```bash
# Создать папку для сборки
mkdir build
cd build

# Конфигурация и компиляция
cmake ..
make -j4

# Запуск
./jsonparser
```

### 2. Генерация тестовых файлов

После запуска выберите:
- **[11] Генератор JSON файлов** - для файлов до 100 МБ
- **[15] Генератор больших файлов (ГБ)** - для файлов 1-10 ГБ

Файлы сохраняются в папку `data/`

### 3. Проверка работы

Выберите **[1] Загрузить JSON файл** и выберите сгенерированный файл.

---

## 📋 Основной функционал

| Пункт меню | Описание |
|------------|----------|
| [1] | Загрузить JSON файл |
| [2] | Показать структуру (дерево) |
| [3] | Поиск по пути (например: user.address.city) |
| [4] | Редактировать значение |
| [5] | Добавить элемент |
| [6] | Удалить элемент |
| [7] | Сохранить в файл |
| [8] | Валидация JSON |
| [9] | Статистика |
| [10] | Метрики выполнения |
| [11] | Генератор JSON файлов (до 100 МБ) |
| [12] | Валидация с подсчётом ошибок |
| [13] | Информация о системе |
| [14] | Многопоточная валидация |
| [15] | Генератор больших файлов (1-10 ГБ) |

---

## ⚡ Особенности

### 1. Работа с большими файлами (> 500 МБ)

При загрузке файла > 500 МБ парсер предлагает выбор:

```
[!] ВНИМАНИЕ: Файл очень большой!

Для файлов такого размера доступны режимы:
  [1] Потоковая валидация (без загрузки в память)
  [2] Попытка загрузить (может привести к нехватке памяти!)
  [3] Отмена

Рекомендуется: [1]
```

- **Режим [1]** - Валидация без загрузки (~50 МБ памяти, ~40 сек для 2 ГБ)
- **Режим [2]** - Толерантная загрузка (пропускает ошибки, загружает валидные элементы)

### 2. Толерантная загрузка

Парсер **НЕ останавливается** при ошибках:
- ✅ Пропускает ошибочные строки
- ✅ Загружает валидные элементы
- ✅ Показывает список ошибок

Пример:
```
[OK] Файл загружен!
Успешно загружено элементов: 1000000
Ошибок при парсинге: 5
Всего строк обработано: 1000005

[!] ОБНАРУЖЕНЫ ОШИБКИ:
  Строка 12345 [PARSER]: Неожиданный токен: Comma
  ...
```

### 3. Показ структуры

Для больших файлов предлагается выбор детализации:
- **[1]** Первые 3 уровня (быстро)
- **[2]** Первые 5 уровней (средне)
- **[3]** До 10 уровней (может быть медленно)
- **[4]** Только корневой уровень (мгновенно)

---

## 🏗️ Архитектура проекта

```
jsonparser/
├── CMakeLists.txt          # Конфигурация сборки
├── README.md               # Этот файл
├── include/                # Заголовочные файлы
│   ├── JsonValue.hpp       # Основной тип данных JSON (std::variant)
│   ├── Lexer.hpp           # Лексический анализатор
│   ├── Parser.hpp          # Парсер (рекурсивный спуск)
│   ├── Serializer.hpp      # Сериализация JSON
│   ├── Generator.hpp       # Генератор тестовых файлов
│   ├── Validator.hpp       # Валидатор JSON
│   ├── ParallelProcessor.hpp  # Многопоточная обработка
│   ├── SystemInfo.hpp      # Информация о системе
│   └── ProgressBar.hpp     # Прогресс-бар для длительных операций
├── src/                    # Реализация
│   ├── main.cpp            # Главный файл с меню
│   ├── JsonValue.cpp
│   ├── Lexer.cpp
│   ├── Parser.cpp
│   ├── Serializer.cpp
│   ├── Generator.cpp
│   ├── Validator.cpp
│   └── ParallelProcessor.cpp
└── data/                   # Папка для JSON файлов
    ├── test_file.json      # Маленький тестовый файл
    └── test_with_errors.json  # Файл с ошибками для теста
```

---

## 🧪 Тестирование

### Тест 1: Генерация файла 100 МБ
```bash
./jsonparser
# [15] Генератор больших файлов (ГБ)
# Размер: 0.1 ГБ (100 МБ)
# Ошибки: 0
```

### Тест 2: Загрузка файла 100 МБ
```bash
# [1] Загрузить JSON файл
# Выбрать сгенерированный файл
# Файл < 500 МБ загрузится автоматически
```

### Тест 3: Показ структуры
```bash
# [2] Показать структуру
# Выбрать уровень детализации: [1]
# Структура показывается мгновенно
```

### Тест 4: Генерация файла с ошибками
```bash
# [15] Генератор больших файлов (ГБ)
# Размер: 0.1 ГБ
# Ошибки: 100
```

### Тест 5: Загрузка файла с ошибками
```bash
# [1] Загрузить JSON файл
# Выбрать файл с ошибками
# Парсер пропустит ошибки и загрузит валидные элементы
```

### Тест 6: Генерация файла 1 ГБ (опционально)
```bash
# [15] Генератор больших файлов (ГБ)
# Размер: 1 ГБ
# Ошибки: 0
# ВНИМАНИЕ: Займёт ~5-10 минут!
```

### Тест 7: Валидация файла 1 ГБ (опционально)
```bash
# [1] Загрузить JSON файл
# Выбрать файл 1 ГБ
# [1] Потоковая валидация
# Результат за ~40 секунд
```

---

## 📊 Производительность

| Операция | Размер | Время | Память |
|----------|--------|-------|--------|
| Генерация файла | 100 МБ | ~10 сек | ~50 МБ |
| Генерация файла | 1 ГБ | ~5 мин | ~50 МБ |
| Загрузка файла | 100 МБ | ~5 сек | ~200 МБ |
| Потоковая валидация | 1 ГБ | ~40 сек | ~50 МБ |
| Толерантная загрузка | 1 ГБ | ~120 сек | ~2 ГБ |
| Показ структуры (3 уровня) | Любой | < 1 сек | - |

---

## 🔧 Требования

- **Компилятор:** C++17 (GCC 7+, Clang 5+, MSVC 2017+)
- **CMake:** 3.12+
- **ОС:** Linux, macOS, Windows
- **Память:** Минимум 4 ГБ (для работы с большими файлами рекомендуется 8+ ГБ)

---

## 📚 Документация

В проекте есть дополнительная документация:

- **USAGE.md** - Подробное руководство по работе с большими файлами
- **IMPROVEMENTS.md** - Описание всех улучшений и оптимизаций
- **TOLERANT_LOADING.md** - Документация по толерантной загрузке
- **FULL_AUDIT.md** - Полный аудит кода (проверка всех функций)

---

## 💡 Советы

1. **Для быстрого теста:** Сгенерируйте файл 10 МБ (пункт [11])
2. **Для демонстрации мощности:** Сгенерируйте файл 100 МБ или 1 ГБ (пункт [15])
3. **Для теста обработки ошибок:** Сгенерируйте файл с ошибками (пункт [15], указать количество ошибок)
4. **Для экономии памяти:** Используйте потоковую валидацию (режим [1])
5. **Для редактирования:** Используйте толерантную загрузку (режим [2])

---

## 🎯 Примеры использования

### Пример 1: Создать файл и посмотреть структуру
```bash
./jsonparser
# [11] Генератор JSON файлов
# Количество объектов: 1000
# Ошибок: 0

# [1] Загрузить JSON файл
# Выбрать сгенерированный файл

# [2] Показать структуру
# Вариант: [1] (3 уровня)
```

### Пример 2: Протестировать обработку ошибок
```bash
./jsonparser
# [15] Генератор больших файлов (ГБ)
# Размер: 0.05 ГБ (50 МБ)
# Ошибок: 50

# [1] Загрузить JSON файл
# Выбрать файл с ошибками
# Файл загрузится, ошибки будут показаны
```

### Пример 3: Проверить производительность
```bash
./jsonparser
# [15] Генератор больших файлов (ГБ)
# Размер: 1 ГБ
# Ошибок: 0

# [1] Загрузить JSON файл
# Выбрать файл 1 ГБ
# [1] Потоковая валидация
# Результат: ~40 сек, ~50 МБ памяти
```

---

## ✅ Что проверить

1. ✅ Генерация файлов разных размеров (10 МБ, 100 МБ, 1 ГБ)
2. ✅ Загрузка файлов < 500 МБ (автоматическая)
3. ✅ Загрузка файлов > 500 МБ (выбор режима)
4. ✅ Показ структуры с разными уровнями детализации
5. ✅ Потоковая валидация больших файлов
6. ✅ Толерантная загрузка файлов с ошибками
7. ✅ Редактирование значений
8. ✅ Сохранение в файл
9. ✅ Статистика и метрики

---

## 🎉 Готово!

Теперь можно:
1. Собрать проект: `mkdir build && cd build && cmake .. && make -j4`
2. Запустить: `./jsonparser`
3. Сгенерировать тестовые файлы: пункт [11] или [15]
4. Проверить все функции

**Удачи! 🚀**
