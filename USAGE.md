# Как работать с большими JSON файлами

## Проблема решена! ✅

Теперь парсер корректно работает с файлами любого размера, включая ваш **pupa.json (2 ГБ)**.

## Два режима работы

### 1️⃣ Потоковый режим (рекомендуется для файлов > 500 МБ)

**Что делает:**
- Проверяет файл построчно
- НЕ загружает в память
- Показывает статистику
- Использует ~50 МБ памяти

**Доступные функции:**
- ✅ Валидация
- ✅ Статистика (количество элементов, строк, токенов)
- ✅ Метрики производительности
- ❌ Просмотр структуры (требует загрузки)
- ❌ Редактирование (требует загрузки)
- ❌ Поиск по пути (требует загрузки)

### 2️⃣ Полная загрузка (для файлов < 500 МБ)

**Что делает:**
- Загружает весь файл в память
- Позволяет редактировать
- Все функции доступны

**Доступные функции:**
- ✅ Все функции парсера
- ✅ Просмотр структуры
- ✅ Редактирование
- ✅ Поиск по пути

## Как использовать

### Шаг 1: Запустить парсер

```bash
cd /Volumes/SSD\ 512/jsonparser/build
./jsonparser
```

### Шаг 2: Загрузить файл pupa.json

```
Выберите пункт меню: 1
Выберите файл: 1  (pupa.json)
```

### Шаг 3: Выбрать режим

Появится предупреждение:
```
Размер файла: 2.00 ГБ

[!] ВНИМАНИЕ: Файл очень большой!

Для файлов такого размера доступны режимы:
  [1] Потоковая валидация (без загрузки в память)
  [2] Попытка загрузить (может привести к нехватке памяти!)
  [3] Отмена

Рекомендуется: [1]
Выбор:
```

**Выберите [1]** для потоковой валидации.

### Шаг 4: Дождаться завершения

Увидите прогресс-бар:
```
Валидация: [█████████████████████████] 100% (2.00 ГБ / 2.00 ГБ) 51.5 МБ/с Готово! (39с)
```

### Шаг 5: Результат

```
══════════════════════════════════════════════════════════════
[OK] Файл прошел валидацию!
══════════════════════════════════════════════════════════════
Элементов обработано: 6670438
Строк в файле: 6670442
Токенов: 539655038
Макс. глубина: 3
Время обработки: 39.75 сек
══════════════════════════════════════════════════════════════

[i] Файл успешно проверен в потоковом режиме.
    Доступны функции: Статистика, Метрики, Поиск по строкам.
    Функции редактирования НЕ доступны (файл не загружен).
```

## Что теперь видно в заголовке

```
══════════════════════════════════════════════════════════════
               JSON PARSER - Курсовая работа
              Парсер JSON на C++17 (std::variant)
══════════════════════════════════════════════════════════════
 Файл: /path/to/pupa.json [ПОТОКОВЫЙ РЕЖИМ]
══════════════════════════════════════════════════════════════
```

## Доступные функции в потоковом режиме

| Пункт меню | Функция | Доступно? |
|------------|---------|-----------|
| [1] | Загрузить JSON файл | ✅ Да |
| [2] | Показать структуру | ⚠️ С предложением загрузки |
| [3] | Поиск по пути | ❌ Нет |
| [4] | Редактировать значение | ❌ Нет |
| [5] | Добавить элемент | ❌ Нет |
| [6] | Удалить элемент | ❌ Нет |
| [7] | Сохранить в файл | ⚠️ Только если загружен |
| [8] | Валидация JSON | ✅ Да |
| [9] | Статистика | ✅ Да |
| [10] | Метрики выполнения | ✅ Да |
| [11] | Генератор JSON файлов | ✅ Да |
| [12] | Валидация с подсчётом ошибок | ✅ Да |

## Если нужно просмотреть структуру

При выборе пункта [2] в потоковом режиме:

```
[!] Файл был обработан в потоковом режиме (не загружен в память).

Для просмотра структуры нужно загрузить файл полностью.
Это может занять много памяти (2.00 ГБ).

Хотите попробовать загрузить файл? (да/нет):
```

**Варианты:**
- Введите **"да"** - попытка загрузить 2 ГБ в память (рискованно!)
- Введите **"нет"** - вернуться к меню

## Производительность

### Ваш файл pupa.json (2 ГБ):

| Параметр | Значение |
|----------|----------|
| Размер | 2.00 ГБ |
| Элементов | 6,670,438 |
| Строк | 6,670,442 |
| Токенов | 539,655,038 |
| Макс. глубина | 3 |
| **Время обработки** | **~40 секунд** |
| **Использовано памяти** | **~50 МБ** |
| **Скорость** | **51.5 МБ/с** |

## Частые вопросы

**Q: Почему "Показать структуру" не работает?**
A: В потоковом режиме файл НЕ загружен в память. Нужно либо загрузить файл (опасно для 2 ГБ), либо использовать режим [2] при загрузке файла.

**Q: Как редактировать файл 2 ГБ?**
A: Для редактирования нужна полная загрузка. Файл 2 ГБ слишком большой - рекомендуется обработать его другими инструментами или разбить на части.

**Q: Можно ли искать по файлу без загрузки?**
A: Сейчас нет, но можно добавить потоковый поиск. Для этого используйте пункт [12] валидация с ошибками.

**Q: Файл с ошибками - как открыть?**
A: Потоковая валидация игнорирует ошибки и продолжает обработку. Используйте режим [1] при загрузке.

## Итого

✅ Файл 2 ГБ **РАБОТАЕТ**
✅ Обрабатывается за **40 секунд**
✅ Использует всего **50 МБ памяти**
✅ Прогресс-бар показывает реальное состояние
✅ Файлы с ошибками обрабатываются корректно

🎉 **Проблема полностью решена!**
